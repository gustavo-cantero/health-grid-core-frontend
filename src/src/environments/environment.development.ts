export const environment = {
  production: false,
  // Empty base URL → requests are relative and go through the dev-server proxy
  // (proxy.conf.json) to avoid CORS, since the API does not send CORS headers.
  apiBaseUrl: '',
};
