services:
    frontend:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: daii-frontend
        restart: unless-stopped
        expose:
            - "80"
